## Reduce EAS upload size / speed up archive
.git
.expo
.eas

node_modules
dist
docs

# Full native trees (EAS prebuilds on the server)
android
ios
apps/*/android
apps/*/ios

# Heavy local artifacts that must never ship in Users builds
apps/admin/dist
apps/admin/.expo-tmp-export
apps/admin/.expo
apps/*/dist
**/.expo-tmp-export

# Android build outputs/caches (big + safe to exclude)
android/.gradle
android/build
android/app/build
android/app/.cxx
android/.cxx

**/*.log
**/.DS_Store
**/Thumbs.db

# local env files (we use `eas.json` env + EAS secrets)
.env
.env.*
