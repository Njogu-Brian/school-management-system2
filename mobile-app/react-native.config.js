/**
 * Used by @react-native/gradle-plugin autolinking (`generateAutolinkingPackageList`).
 * Must match `android.defaultConfig.applicationId` / `app.config.js` expo.android.package.
 */
module.exports = {
  project: {
    android: {
      packageName: 'com.schoolerp',
    },
  },
};
