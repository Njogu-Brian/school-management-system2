name: Admin production Android

on:
  workflow_dispatch: {}

jobs:
  build_android:
    name: Build Admin Android AAB
    type: build
    params:
      platform: android
      profile: production
      message: Admin Play Store production AAB
