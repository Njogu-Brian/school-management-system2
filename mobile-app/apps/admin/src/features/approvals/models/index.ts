/**
 * Canonical approval domain models live in `@erp/core` (`ApprovalItem`, statuses, priorities).
 * Re-exported here for the admin feature boundary.
 */
export type {
  ApprovalCompositeId,
  ApprovalItem,
  ApprovalListFilters,
  ApprovalPriority,
  ApprovalSourceType,
  ApprovalStatus,
} from '@erp/core';
