; PHP runtime limits for this app (PHP-FPM / CGI SAPI).
; Needed so the M-Pesa statement analyzer can accept large annual PDFs
; and finish parsing (which can take 1-3 minutes).
upload_max_filesize = 25M
post_max_size = 30M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
